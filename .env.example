# Copy to .env and fill in real values. .env is gitignored; never commit keys.

# OpenAI (extraction/adjudication candidates, judge, smoke test)
OPENAI_API_KEY=

# OpenRouter (open-weight models: DeepSeek-V3.2, Qwen3.5-9B reader, embeddings)
OPENROUTER_API_KEY=
